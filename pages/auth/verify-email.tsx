import{useRouter}from"next/router";import{useEffect,useState}from"react";import api from"../../utils/api";export default function Verify(){const r=useRouter();const[m,setM]=useState("Verifying...");useEffect(()=>{if(r.isReady)api.post("/auth/verify-email/",{token:r.query.token}).then(x=>setM(x.data.message)).catch(e=>setM(e?.response?.data?.detail||"Verification failed"))},[r.isReady]);return <main className="p-8 text-center"><h1 className="text-2xl font-bold">RSJH Email Verification</h1><p>{m}</p></main>}