# RSJH backend environment (LOCAL DEVELOPMENT template)
# Generate with: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=replace-with-a-long-random-secret
DEBUG=True
DJANGO_ENV=development
ALLOWED_HOSTS=localhost,127.0.0.1
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
CSRF_TRUSTED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
FRONTEND_URL=http://localhost:3000

# PostgreSQL
DB_NAME=rmsj
DB_USER=postgres
DB_PASSWORD=Vasumati1@
DB_HOST=localhost
DB_PORT=5432

# Gmail SMTP for RSJH editorial email
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=researchrwandahub@gmail.com
EMAIL_HOST_PASSWORD=ibih xpaz ifcr asee
DEFAULT_FROM_EMAIL=Rwanda Student Journal for Health <researchrwandahub@gmail.com>
SUPPORT_WHATSAPP_NUMBER=+250736662338

# Production web security (Render/production deploy only — also set
# DJANGO_ENV=production, otherwise these values are ignored locally)
SECURE_SSL_REDIRECT=True
SECURE_HSTS_SECONDS=31536000

SECRET_KEY=your-existing-secret
DEBUG=True
DJANGO_ENV=development

GOOGLE_CLIENT_ID=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.apps.googleusercontent.com